<h1>RAM</h1>
RAM, or Read-Write Memory, is the computer’s main memory that temporarily stores data and enables fast read and write operations. Located on the motherboard, it directly supports the CPU, allowing programs to run smoothly and information to be processed quickly.

As volatile memory, data in RAM is lost when the computer is powered off.
Its high speed greatly influences overall system performance, and insufficient RAM can slow program loading and reduce efficiency.

<h1>Working of RAM</h1>
RAM (Random Access Memory) is made of tiny transistors and capacitors that store electrical charges representing data bits. It acts as the computer’s short-term memory, temporarily holding data the CPU needs to access quickly.

Temporary Storage: RAM holds active files and programs for fast access.
Volatile: Data is lost when the power is turned off, so saving to permanent storage (hard drive or SSD) is essential.
High Speed: RAM is much faster than other storage, allowing the CPU to process data efficiently.

Data or Programme first stored on the hard drive. then from the hard drive its loaded in the ram. When its loaded in the ram, cpu can acees the data. 
Ram requires constant eletrical power to store data. Ram come in different types. As, 
<h2>DRAM</h2> stands for Dynamic Ram
DRAM is a memory that contains capacitores. A capacitores is like a small bucket that stores electricity. Also these capacitors hold bit of info such as 1s or 0s
Capacitors have to be refreshed with electricity. 

<h2>SDRAM</h2> stands for snchronous dynamic ram these type of memory are used in today in Ram DIMMs
DRAM operates asynchrounously with the system clock which means it runs slower than the system clock.
Whereas SDRAM operates synchrouously with the system clock and so it is faster.
So RAM is stored on the motherboard in dmodule called DIMMs.
DIMMs have different size of memory from 128 mb to 32 GB per DIMMs

The term 64 or 32 bit data path refers to the number of bits of data that are tranferred in 1 clock cycle.
DIMMS have a 64 bit data path. Older Ram module called SIMMs it has 32 bit data path. Tha'ts why DIMMs is faster.

A single bit or 1 bit of data is the smallest form of data that the computer reads. because in the computing world, c computer only understnad 1s and os. which is presented by a single bit of data.

So if a memory DIMMs is rated to have a 64-bit data path, then that means it has an 8 byte wide data path or bus. 
 8 bits =  1 byte
 64/8 = 8 byte wide bus

 SDRAM has fifferent speeds. In 1990s, it labeled pc-100 which means 100Mhz maximum speed it can operate. As SDRAM only have 
 64-bit modules it has an 8 byte wide bus
 so total bandwidth of pc-100 is
       100 MHz * 8 bytes = 800 MB/s

so pc-100 ram can transfer data at a maximum rate of 800 megabytes per second. 

<h2>RDRAM</h2> later comes with maximum 800 MHz speed which is lot faster than SDRAM, but RDRAM only had a 2 byte wide bus

As technology advanced and speed become faster, new tachnology comes in caleed DDR stand for double data rate. DDR sends doube amount of data in each clock signal
Non-DDR Ram only use the rising edge of the clock singal to tranfer data. But DDR uses both the rising and falling edge. Which means DDR cam send twince amount of data ata sinle bit. 


DDR Ram DIMMs labeled both clock speed and the toal bandwidth in its name. 

DDR-333 MHz             PC-2700

Later a new technology has advanced DDR called DDR2 and it allows higher bus speeds and send twice amount of data than DDR

<h2>DDR2</h2> dimms has 240 pins DDR has 184 pins.


"RAM consists of a list of addresses"

Imagine RAM like a hotel.

Each room has:

Room number = Address
Something stored inside = Data


So RAM is basically:

millions/billions of storage locations

Each location has:

a unique address

